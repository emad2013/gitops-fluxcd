apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: monitoring        # ← must have this
namespace: monitoring
resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
patches:
  - path: patch-namespace-psa.yaml
    target:
      kind: Namespace
      name: monitoring