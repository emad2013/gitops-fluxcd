apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: webapp-dev
namespace: webapp
resources:
  - ../base          # ← changed from bases to resources
patches:
  - path: patch-deployment.yaml
    target:
      kind: Deployment
      name: webapp