apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: webapp-dev
namespace: webapp
bases:
  - ../../base          # ← points to apps/webapp/base/
patches:
  - path: patch-deployment.yaml
    target:
      kind: Deployment
      name: webapp