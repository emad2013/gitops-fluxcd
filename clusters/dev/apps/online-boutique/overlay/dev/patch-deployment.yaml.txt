apiVersion: apps/v1
kind: Deployment
metadata:
  name: online-boutique
  namespace: online-boutique
spec:
  replicas: 2                  # ← override replicas from 1 to 2
  template:
    spec:
      containers:
        - name: nginx
          image: nginx:1.25.3  # ← pin to specific version instead of latest
          resources:
            requests:
              cpu: "200m"      # ← increase cpu request
              memory: "256Mi"  # ← increase memory request
            limits:
              cpu: "500m"      # ← increase cpu limit
              memory: "512Mi"  # ← increase memory limit